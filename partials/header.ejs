<!DOCTYPE>
<html>
    <head>
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">        <title>To-do</title>
    </head>
    <body>
        <div class="container">
            <% if(currentUser != undefined){%>
                <ul class="nav justify-content-end">
                    <li class="nav-item">
                      <p> Sign up as <%= currentUser.username %> <span><a  href="/logout">Logout </a></span></p>
                    </li>
                </ul>
            <% } %>
            <div class="container">
             <% if (error && error.length > 0 ) {%>
                <div class="alert alert-danger" role="alert">
                        <%=error%>
                        <% }; %>
                </div>
            <% if (success && success.length > 0 ) {  %>
                <div class="alert alert-success" role="alert">
                        <%=success%>
                 </div>
            <% }; %>
            
           